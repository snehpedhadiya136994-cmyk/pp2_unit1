#include <stdio.h>

void calculateArea(float r, float *area) {
    *area = 3.14159 * r * r;
}

int main() {
    float radius, area;
    printf("Enter radius: ");
    scanf("%f", &radius);
    calculateArea(radius, &area);
    printf("Area of circle: %.2f\n", area);
    return 0;
}