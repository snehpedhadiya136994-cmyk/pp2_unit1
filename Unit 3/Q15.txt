#include <stdio.h>
#include <string.h>

int main() {
    char s1[20], s2[20];
    printf("Enter source string: "); scanf("%s", s1);
    strcpy(s2, s1);
    printf("Copied string: %s\n", s2);
    return 0;
}