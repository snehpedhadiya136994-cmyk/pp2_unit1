#include <stdio.h>
#include <string.h>

int main() {
    char s1[20], s2[20];
    scanf("%s %s", s1, s2);
    if(strcmp(s1, s2) == 0) printf("Strings are same\n");
    else printf("Strings are not same\n");
    return 0;
}