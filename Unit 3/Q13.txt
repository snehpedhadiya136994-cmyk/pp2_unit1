#include <stdio.h>
#include <string.h>

int main() {
    char s[20];
    printf("Enter string: "); scanf("%s", s);
    int len = strlen(s);
    for(int i = len-1; i >= 0; i--) printf("%c", s[i]);
    return 0;
}